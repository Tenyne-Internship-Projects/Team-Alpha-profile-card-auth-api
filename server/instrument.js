// //@ Load environment variables from the .env file
// require("dotenv").config();

// //@ Import the Sentry library (used for error tracking)
// const Sentry = require("@sentry/node");

// //@ Initialize Sentry with your project's DSN (Data Source Name)
// Sentry.init({
//   dsn: process.env.SENTRY_DSN, // Sentry DSN from .env
//   sendDefaultPii: true, // Send user-related data (like email) if available
// });
